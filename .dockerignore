# Build artefacts and VCS
target/
**/target/
.git/
.gitignore

# Not needed inside the image
docs/
k8s/
.github/
blobs/

# Local env
.env
.env.local
*.log
.DS_Store
# UI build artefacts: the image builds the export itself.
ui/node_modules
ui/.next
ui/out
